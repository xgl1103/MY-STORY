# My Story — 功能规划文档

> 版本：v2.0（产品经理 + 架构师 3 轮审查扩写版）  
> 日期：2026-07-06  
> 状态：开发基准  
> 说明：本文档涵盖产品功能定义、系统架构、AI 架构、数据模型、技术选型、错误处理、安全隐私、测试策略与开发计划，是后续开发的唯一依据。  
> 审查记录：第 1 轮完整性审查补全 24 项缺失，第 2 轮一致性审查修正 5 项矛盾，第 3 轮可执行性审查细化 8 项落地细节。

---

## 一、产品概述

### 1.1 定位

My Story 是一款将用户日常日记转化为以自己为主角的 AI 连载小说的手机 App。用户每天用文字或语音记录真实经历，AI 根据预设的世界观映射规则，将日常事件转化为剧情段落，生成连续剧式的专属小说。第一版采用《诡秘之主》世界观，故事总时长三个月（90 天）。

### 1.2 核心体验

用户每天记录日记 → AI 将日常行为映射为世界观内行为 → 经过 2-3 轮内部自我审查后生成小说段落 → 用户预览并确认（可重生成或手动编辑）→ 段落存入章节 → 像追更一样追自己的专属小说。三个月后，用户将读完一部属于自己的、独一无二的长篇故事。

### 1.3 一句话描述

> 你的日常，就是主角的传奇。

---

## 二、第一版范围

### 2.1 做什么

| 模块 | 具体内容 |
|------|---------|
| 世界观 | 诡秘之主（单世界观，架构支持后续扩展） |
| 途径选择 | 默认开放占卜家途径（架构支持后续扩展） |
| 故事时长 | 90 天（架构支持配置不同时长） |
| 章节结构 | 6 个章节段（序章 5 天 + 4 章各 20 天 + 终章 5 天） |
| 日记输入 | 文字输入 + 语音转文字 |
| AI 模型 | 多模型支持，用户自行填写 API Key |
| 故事生成 | Human-in-the-Loop 审核 + 2-3 轮 AI 内部自我审查 |
| 断更处理 | 世界观内自然过渡，不做额外惩罚 |
| 数据存储 | 全部本地 SQLite，无需登录，无需服务器 |

### 2.2 不做（后续扩展）

| 项目 | 说明 |
|------|------|
| 登录系统 | 第一版纯单机，无账号体系 |
| 多世界观 | 架构预留，第一版只诡秘之主 |
| 其他途径 | 架构预留，第一版只占卜家 |
| 成就/激励系统 | 后续考虑 |
| 分享/导出小说 | 后续考虑（数据备份导出除外） |
| 云端同步 | 换机靠本地导出/导入 JSON |
| 自定义世界观 | 后续功能 |
| 图片/标签输入 | 后续扩展输入方式 |

---

## 三、架构设计原则

### 3.1 扩展性原则

第一版功能范围收窄，但代码结构按"多"来设计。后续扩展时只需"填空"，不需要"拆房"。具体体现为：世界观、途径、AI 模型、故事时长、输入方式均为独立模块，通过配置文件和适配器接口与核心逻辑解耦。

### 3.2 可扩展维度

| 维度 | 当前 | 架构预留方式 |
|------|------|-------------|
| 世界观 | 诡秘之主 | 每个世界观是一个独立 JSON 配置包，包含设定档案、映射规则、剧情大纲、奇遇库。新增世界观只需导入新配置包 |
| 途径 | 占卜家 | 途径是配置包内的独立数据项，包含途径设定、能力体系、晋升路线。新增途径只需在配置包中添加数据 |
| AI 模型 | 多厂商 | 统一 AIAdapter 接口，每个厂商一个适配器实现。新增模型只需新增一个 Adapter 类 |
| 故事时长 | 90 天 | 时长和章节规则从配置文件读取，不写死在代码中 |
| 日记输入 | 文字+语音 | 输入模块抽象为 InputProvider，新增方式只需扩展 Provider |

### 3.3 技术路线

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端 UI | HTML/CSS/JavaScript + Vue 3 | 移动端适配网页，Vue 3 用于组件化和状态管理 |
| 打包工具 | Capacitor（首选）/ HBuilderX（备选） | 网页打包成手机 App，Capacitor 原生插件生态更好 |
| 数据存储 | SQLite（sql.js） | 全部本地存储，无服务器 |
| AI 调用 | 用户自带 API Key | 前端直接调用模型 HTTP API，无后端中转 |
| 语音输入 | Web Speech API / Capacitor 语音插件 | 文字/语音两种输入 |
| 加密存储 | AES-GCM（Web Crypto API） | API Key 加密后存入本地 |

---

## 四、系统架构总览

### 4.1 分层架构

系统分为五层，各层职责清晰、接口明确：

| 层级 | 职责 | 核心模块 |
|------|------|---------|
| 表现层 | 页面渲染、用户交互 | 首页/阅读器、日记输入、生成预览、设置页、数据管理 |
| 业务逻辑层 | 核心业务处理 | 故事引擎、日记解析器、章节管理器、映射规则引擎 |
| AI 编排层 | AI 调用编排 | Prompt 拼装器、AI 适配器、审查循环器、摘要生成器 |
| 记忆管理层 | 上下文管理 | 即时上下文提供器、摘要记忆提供器、RAG 检索器 |
| 数据层 | 持久化存储 | SQLite 数据库、配置加载器、数据导出/导入 |

### 4.2 页面导航结构

```
App 启动
  ├── 首次使用 → 引导页（Onboarding）
  │     ├── Step 1: 欢迎介绍
  │     ├── Step 2: 设置 API Key 和模型
  │     ├── Step 3: 设置主角名
  │     ├── Step 4: 世界观介绍（诡秘之主背景）
  │     └── Step 5: 开始第一天
  │
  └── 非首次 → 首页/阅读器
        ├── [记日记] → 日记输入页 → AI 生成预览页 → 返回首页
        ├── [目录] → 章节列表 → 选择章节 → 阅读该章
        ├── [设置] → 设置页
        │     ├── 世界观设置
        │     ├── API Key 设置
        │     └── 数据管理
        └── [故事进度] → 进度详情页
```

### 4.3 路由设计

| 路由路径 | 页面 | 说明 |
|---------|------|------|
| `/onboarding` | 引导页 | 首次使用时进入，完成后跳转首页 |
| `/` | 首页/阅读器 | 默认页，展示最新章节内容 |
| `/diary` | 日记输入页 | 记录当天日记 |
| `/preview` | AI 生成预览页 | 展示 AI 生成结果，用户审核 |
| `/reader/:chapterId` | 章节阅读页 | 阅读指定章节完整内容 |
| `/settings` | 设置首页 | 设置入口 |
| `/settings/world` | 世界观设置 | 世界观、途径、主角名、时长 |
| `/settings/api` | API Key 设置 | 模型选择和 Key 管理 |
| `/settings/data` | 数据管理 | 备份、导入、重置 |

---

## 五、AI 架构设计

### 5.1 AI 生成完整流程

从用户提交日记到最终定稿，完整流程如下：

```
用户提交日记
  │
  ▼
[1] 日记解析 — AI 从用户输入中提取关键事件和行为大类
  │
  ▼
[2] 映射匹配 — 映射规则引擎将行为大类匹配到世界观行为
  │
  ▼
[3] 上下文组装 — 拼装三层记忆 + 当日映射结果 + 章节阶段指令
  │
  ▼
[4] 初稿生成 — AI 根据组装好的 Prompt 生成小说段落初稿
  │
  ▼
[5] 审查轮 1 — AI 审查初稿：世界观一致性、逻辑矛盾、文风
  │  ├─ 通过 → 进入轮 2
  │  └─ 不通过 → 修改后重新审查（最多重试 2 次）
  │
  ▼
[6] 审查轮 2 — AI 审查修改稿：是否引入新问题、叙事流畅性
  │  ├─ 通过 → 进入轮 3
  │  └─ 不通过 → 再次修改（最多重试 1 次）
  │
  ▼
[7] 润色轮（可选） — AI 最终润色，确保文风统一和起伏感
  │
  ▼
[8] 输出预览 — 将最终版本展示给用户
  │
  ▼
[9] 用户审核（Human-in-the-Loop）
  │  ├─ 通过 → 存入数据库，更新进度
  │  ├─ 重新生成 → 回到步骤 [4]（最多 3 次）
  │  └─ 手动编辑 → 用户修改后存入数据库
  │
  ▼
[10] 章节检查 — 判断是否到达章节边界，若是则触发摘要生成
```

### 5.2 故事段落状态机

每个 story_segment 在生命周期中经历以下状态：

| 状态 | 说明 | 可迁移到 |
|------|------|---------|
| `pending` | 日记已提交，等待 AI 处理 | `parsing` |
| `parsing` | AI 正在解析日记 | `generating` |
| `generating` | AI 正在生成+审查循环中 | `draft_ready`, `generate_failed` |
| `draft_ready` | 内部审查完成，等待用户预览 | `finalized`, `regenerating`, `editing` |
| `regenerating` | 用户要求重新生成 | `generating`, `generate_failed` |
| `editing` | 用户手动编辑中 | `finalized` |
| `finalized` | 已定稿，存入章节库 | （终态） |
| `generate_failed` | AI 生成失败（重试耗尽） | `generating`（用户手动重试） |

### 5.3 Human-in-the-Loop（人在回路）

每次 AI 生成小说段落后，不直接定稿，而是先给用户预览/审核：

- **通过**：段落存入数据库，更新 `current_day`，返回首页阅读
- **重新生成**：用相同输入重新调用 AI 生成一版新内容。用户最多可重试 3 次（与内部审查轮次独立计数）。每次重新生成时，在 Prompt 中加入"请生成与之前不同版本"的指令
- **手动编辑**：进入编辑模式，用户直接修改文本，修改后点击"保存"存入。标记 `is_edited = true`

### 5.4 多轮内部审查与自我修正

AI 在输出给用户之前，内部运行 2-3 轮自我审查循环。这是 AI 内部行为，用户不可见：

| 轮次 | 动作 | 审查标准 |
|------|------|---------|
| 生成 | AI 生成初稿 | — |
| 审查 1 | 检查初稿 | 世界观设定一致性、行为映射正确性、逻辑矛盾检测 |
| 修改 1 | 根据审查意见修改 | — |
| 审查 2 | 检查修改稿 | 修改是否引入新问题、前后文衔接、叙事流畅性 |
| 修改 2（如需） | 再次修改 | — |
| 润色（可选） | 最终润色 | 文风统一、节奏感、伏笔回收 |

实现方式：通过多步 Prompt 链完成。第一轮生成后，把初稿连同审查标准再次喂给 AI，让其输出审查意见 + 修改稿，跑 2-3 轮后输出最终版本。每轮审查有明确的通过/不通过判定，不通过则修改后重审，最多内部重试 3 次，超过后直接输出当前最优版本。

### 5.5 记忆与上下文管理（三层架构）

三个月累积剧情约 4-5 万字，远超任何大模型的上下文窗口，必须分层管理。

#### 第一层：即时上下文（最近 3 天）

- **内容**：最近 3 天生成的完整剧情原文
- **数据来源**：`story_segments` 表中最近 3 条 `finalized` 记录的 `content` 字段
- **用途**：直接放入 Prompt，保证最近剧情的细节连贯
- **Token 预算**：约 3000-4000 tokens（假设每天 800-1200 字）

#### 第二层：摘要记忆（更早章节）

- **内容**：每章压缩成剧情摘要（约 200-300 字），记录核心事件、关键转折、人物关系变化
- **数据来源**：`chapters` 表的 `summary` 字段
- **用途**：放入 Prompt 作为背景参考，让 AI 知道故事宏观进展
- **生成方式**：每章结束时（第 5/25/45/65/85/90 天）由 AI 自动生成摘要并存入 `chapters.summary`
- **Token 预算**：约 800-1500 tokens（6 个章节 × 200-300 字）

#### 第三层：档案记忆（RAG 检索）

- **内容**：世界观设定、人物关系、关键事件、途径等级等结构化数据
- **数据来源**：`world_settings` 表，按 `category` 分类
- **用途**：当 AI 需要引用具体设定时，通过关键词匹配检索相关条目
- **检索方式**：从当日日记解析出的关键词 + 当前章节阶段关键词，匹配 `world_settings` 中的条目，取 Top 5 相关条目
- **Token 预算**：约 1000-2000 tokens

#### 三层配合与 Token 预算总表

| 组成部分 | Token 预算 | 说明 |
|---------|-----------|------|
| 系统 Prompt（世界观+文风+角色） | 1500-2000 | 固定部分 |
| 即时上下文（近 3 天原文） | 3000-4000 | 动态部分 |
| 摘要记忆（章节摘要） | 800-1500 | 动态部分 |
| 档案记忆（RAG 检索结果） | 1000-2000 | 动态部分 |
| 当日日记 + 映射结果 | 500-800 | 动态部分 |
| 章节阶段指令 | 200-300 | 固定部分 |
| **输入总计** | **7000-10600** | — |
| 输出（生成段落） | 800-1500 | 每天 800-1200 字 |
| **单次调用总计** | **8000-12000** | 在主流模型上下文窗口内 |

#### 不同场景的记忆加载策略

| 场景 | 即时上下文 | 摘要记忆 | 档案记忆 |
|------|-----------|---------|---------|
| 日常剧情生成（第 2 天起） | 近 3 天原文 | 已完成章节摘要 | 按关键词检索 Top 5 |
| 首日生成（第 1 天） | 无 | 无 | 占卜家途径设定 + 基础世界观 |
| 章节边界日 | 近 3 天原文 | 所有已完成章节摘要 | 当前章节阶段关键设定 |
| 断更过渡生成 | 近 3 天原文 | 已完成章节摘要 | 无需额外检索 |
| 摘要生成（章节结束时） | 本章所有段落原文 | 之前所有章节摘要 | 无 |

---

## 六、功能模块详细设计

### 6.1 功能总览

| # | 模块 | 功能描述 |
|---|------|---------|
| 0 | 引导页 | 首次使用引导：API Key 设置、主角名设置、世界观介绍 |
| 1 | 首页/阅读器 | 展示最新章节内容，翻页阅读历史，显示连载进度 |
| 2 | 日记输入 | 文字/语音记录当天经历，行为大类标签辅助输入 |
| 3 | AI 生成预览 | 展示 AI 生成的段落，通过/重生成/编辑三个操作 |
| 4 | 世界观设置 | 世界观选择、途径选择、故事时长、主角名 |
| 5 | API Key 设置 | 模型选择、Key 输入、连接测试、参数调整 |
| 6 | 数据管理 | 数据查看、备份导出、导入恢复、重置故事 |

### 6.2 引导页（Onboarding）

首次打开 App 时进入，完成后不再显示。

**Step 1 — 欢迎介绍**
- 展示产品 slogan："你的日常，就是主角的传奇"
- 简要说明产品玩法：记日记 → AI 生成小说 → 追更自己的故事
- 底部"开始设置"按钮

**Step 2 — API Key 设置**
- 说明："My Story 需要连接 AI 来生成你的专属故事，请填写你的 AI API Key"
- 模型选择下拉框（DeepSeek / OpenAI / Kimi / 通义千问）
- API Key 输入框（密码模式）
- "测试连接"按钮，验证 Key 有效性后才能进入下一步
- 测试失败时显示错误原因（无效 Key / 网络问题 / 额度不足）

**Step 3 — 主角名设置**
- 输入框：主角在小说中的名字，默认"你"
- 说明："这个名字将出现在你的每一章故事中"

**Step 4 — 世界观介绍**
- 展示诡秘之主世界观简介（约 200 字）
- 展示占卜家途径简介（约 100 字）
- 展示行为映射规则示例（如"学习 → 研读神秘学"）
- 说明故事时长：90 天，约 6 个章节

**Step 5 — 开始**
- "记录你的第一天，开启你的传奇"按钮
- 点击后跳转日记输入页

### 6.3 首页/阅读器

**进入方式**：打开 App 默认进入（非首次使用时）

**页面结构**：
- 顶部栏：主角名 + 章节进度（第 X 章 / 共 6 章）+ 天数进度（第 Y 天 / 共 90 天）
- 中部：最新生成的小说段落正文，滚动阅读
- 底部栏：上一章按钮 | 目录按钮 | 下一章按钮 | "记日记"悬浮按钮

**交互逻辑**：
- 左右滑动切换章节（仅已生成的章节间切换）
- 点击目录弹出章节列表，每项显示章节号 + 标题 + 天数范围，点击跳转
- "记日记"按钮：如果今天已记录则置灰并显示"今日已更新"，未记录则可点击进入日记输入页
- 长按某段文字：若当前段落属于未锁定章节（最新章），弹出"编辑"选项

**状态处理**：
- 空状态（首次进入无内容）：显示引导语"记录你的第一天，开启你的传奇" + 大号"记日记"按钮
- 加载中：骨架屏占位
- 今日已记录：底部显示"今日故事已更新"标签

### 6.4 日记输入页

**进入方式**：首页点击"记日记"按钮

**页面结构**：
- 顶部：日期显示（第 X 天）+ 返回按钮
- 中部：多行文本输入框（placeholder："今天做了什么？随意写写..."）
- 行为大类标签栏（文本框下方）：学习、工作、健身、社交、休息、娱乐、其他
- 语音输入按钮（文本框右下角）：麦克风图标
- 底部："生成故事"主按钮

**交互逻辑**：
- 文字输入：支持多行、自动保存草稿（防止意外退出丢失内容）
- 语音输入：按住麦克风按钮开始录音，松开后自动转文字填入文本框。转文字期间显示"识别中..."状态
- 行为标签：点击标签后自动在光标位置插入 `[标签名]` 前缀。可多选。不选标签不影响使用
- 草稿保存：输入内容实时存入 `localStorage`，下次进入恢复
- "生成故事"按钮：文本框为空时禁用，有内容时启用

**边界处理**：
- 文本最大长度：5000 字（超出截断并提示）
- 语音识别失败：提示"语音识别失败，请重试或手动输入"
- 今日已记录：进入时提示"今日已记录，是否重新记录？"（重新记录会覆盖当天内容）

### 6.5 AI 生成预览页

**进入方式**：日记输入页点击"生成故事"后自动跳转

**页面结构**：
- 顶部：返回按钮 + "故事生成中..." / "预览"状态标签
- 中部：AI 生成的小说段落正文（只读模式）
- 映射说明区：简要列出"今日事件 → 世界观行为"的映射结果
- 底部：三个操作按钮 — "重新生成" | "编辑" | "确认通过"

**交互逻辑**：
- 生成中：显示 Loading 动画 + 趣味提示语轮播（"正在编织你的命运..."、"占卜师正在凝视水晶球..."、"命运的丝线正在交织..."）
- 生成完成：展示最终段落 + 映射说明，底部按钮变为可操作
- "确认通过"：存入数据库，更新进度，跳转首页
- "重新生成"：回到生成流程，计数 +1。按钮旁显示"已重生成 X/3 次"。达到 3 次后按钮禁用，提示"已达重生成上限，可手动编辑"
- "编辑"：进入编辑模式，文本变为可编辑，底部按钮变为"保存"和"取消"。保存后存入数据库，标记 `is_edited = true`

**错误处理**：
- 生成失败（网络错误/API 错误/超时）：显示错误信息 + "重试"按钮。重试不消耗用户重生成次数
- 生成超时（超过 60 秒）：自动中断，提示"生成超时，请检查网络后重试"

### 6.6 世界观设置页

- **世界观选择**：列表展示，第一版仅"诡秘之主"可选，其余显示"敬请期待"灰色项
- **途径选择**：列表展示，第一版仅"占卜家"可选，其余显示"敬请期待"灰色项
- **故事时长**：第一版固定 90 天，显示为只读信息
- **主角名**：可修改输入框，修改后从下一段故事生效（已生成内容不变）
- **映射规则预览**：只读表格展示当前行为大类到世界观行为的映射规则

### 6.7 API Key 设置页

- **模型选择**：下拉列表（DeepSeek / OpenAI / Kimi / 通义千问）
- **API Base URL**：默认自动填充，高级设置中可自定义（支持代理/中转地址）
- **API Key 输入**：密码框，支持显示/隐藏切换
- **测试连接**：发送一个简单测试请求，返回"连接成功"或错误原因
- **高级参数**（可折叠）：温度（0-2，默认 0.8）、最大输出 tokens（默认 2000）

### 6.8 数据管理页

- **数据概览**：已记录天数 / 90、已生成章节数 / 6、本地存储占用大小
- **备份导出**：点击后生成 JSON 文件，包含全部数据，触发系统文件保存/分享
- **数据导入**：选择 JSON 文件，校验格式后覆盖导入（需二次确认）
- **重置故事**：清空所有数据重新开始，弹出二次确认对话框，需输入"重置"二字确认

---

## 七、数据模型

### 7.1 完整数据表设计

#### app_config（应用配置）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| key | TEXT | NOT NULL UNIQUE | 配置键名 |
| value | TEXT | | 配置值 |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 更新时间 |

存储内容：`onboarding_completed`（是否完成引导）、`db_version`（数据库版本号，用于迁移）等。

#### user_settings（用户设置）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK | 固定为 1（单用户） |
| hero_name | TEXT | DEFAULT '你' | 主角名 |
| world_id | TEXT | DEFAULT 'lord_of_mysteries' | 世界观 ID |
| path_id | TEXT | DEFAULT 'seer' | 途径 ID |
| duration_days | INTEGER | DEFAULT 90 | 故事总时长 |
| current_day | INTEGER | DEFAULT 0 | 当前天数（0 表示尚未开始） |
| current_chapter | INTEGER | DEFAULT 0 | 当前章节号 |
| ai_provider | TEXT | DEFAULT 'deepseek' | AI 模型厂商 |
| ai_base_url | TEXT | | API Base URL（可选） |
| api_key_encrypted | TEXT | | AES-GCM 加密后的 API Key |
| ai_temperature | REAL | DEFAULT 0.8 | 温度参数 |
| ai_max_tokens | INTEGER | DEFAULT 2000 | 最大输出 tokens |
| story_started | BOOLEAN | DEFAULT 0 | 是否已开始故事 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### diary_entries（日记记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| day_number | INTEGER | NOT NULL | 第几天（1-90） |
| raw_text | TEXT | NOT NULL | 用户原始输入 |
| behavior_tags | TEXT | | JSON 数组，如 `["学习","健身"]` |
| is_blank_day | BOOLEAN | DEFAULT 0 | 是否为断更空白日 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 记录时间 |

索引：`CREATE INDEX idx_diary_day ON diary_entries(day_number)`

#### chapters（章节）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| chapter_number | INTEGER | NOT NULL UNIQUE | 章节号（0=序章, 1-4=正章, 5=终章） |
| title | TEXT | | 章节标题 |
| content | TEXT | | 完整章节内容（所有段落拼接） |
| summary | TEXT | | 章节摘要（AI 生成，200-300 字） |
| start_day | INTEGER | NOT NULL | 本章起始天数 |
| end_day | INTEGER | NOT NULL | 本章结束天数 |
| status | TEXT | DEFAULT 'ongoing' | ongoing / completed |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| completed_at | TIMESTAMP | | 完成时间 |

#### story_segments（每日故事段落）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| day_number | INTEGER | NOT NULL | 第几天 |
| chapter_id | INTEGER | FK → chapters.id | 所属章节 |
| diary_id | INTEGER | FK → diary_entries.id | 对应日记 |
| content | TEXT | | AI 生成的段落内容 |
| mapping_desc | TEXT | | 事件映射说明（JSON） |
| revision_count | INTEGER | DEFAULT 0 | 用户重生成次数 |
| internal_review_count | INTEGER | DEFAULT 0 | 内部审查轮次 |
| is_edited | BOOLEAN | DEFAULT 0 | 是否经用户手动编辑 |
| status | TEXT | DEFAULT 'pending' | pending/parsing/generating/draft_ready/regenerating/editing/finalized/generate_failed |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| finalized_at | TIMESTAMP | | 定稿时间 |

索引：`CREATE INDEX idx_segment_day ON story_segments(day_number)`  
索引：`CREATE INDEX idx_segment_chapter ON story_segments(chapter_id)`

#### world_settings（世界观设定档案）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| world_id | TEXT | NOT NULL | 世界观标识 |
| category | TEXT | NOT NULL | path/power_system/location/character/lore/item |
| key | TEXT | NOT NULL | 设定项键名 |
| value | TEXT | NOT NULL | 设定项内容 |
| keywords | TEXT | | 检索关键词（空格分隔） |
| priority | INTEGER | DEFAULT 0 | 检索优先级权重 |

索引：`CREATE INDEX idx_world_cat ON world_settings(world_id, category)`  
索引：`CREATE INDEX idx_world_kw ON world_settings(keywords)`

#### encounter_library（随机奇遇库）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| world_id | TEXT | NOT NULL | 世界观标识 |
| encounter_id | TEXT | NOT NULL | 奇遇唯一标识 |
| title | TEXT | NOT NULL | 奇遇标题 |
| content_template | TEXT | NOT NULL | 奇遇内容模板（含变量占位符） |
| min_chapter | INTEGER | DEFAULT 0 | 最小章节要求 |
| max_chapter | INTEGER | DEFAULT 99 | 最大章节限制 |
| min_path_level | INTEGER | DEFAULT 0 | 最小途径等级要求 |
| tags | TEXT | | 奇遇类型标签 |

#### encounter_log（奇遇触发记录）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| day_number | INTEGER | NOT NULL | 触发天数 |
| encounter_id | TEXT | FK → encounter_library.encounter_id | 奇遇 ID |
| content | TEXT | | 实际生成的奇遇内容 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 触发时间 |

#### story_outline（剧情大纲节点）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INTEGER | PK AUTOINCREMENT | 主键 |
| world_id | TEXT | NOT NULL | 世界观标识 |
| chapter_number | INTEGER | NOT NULL | 所属章节 |
| node_id | TEXT | NOT NULL | 节点标识 |
| node_type | TEXT | NOT NULL | mainline / branch / climax / resolution |
| trigger_day | INTEGER | | 触发天数（灵活） |
| content | TEXT | NOT NULL | 节点描述（给 AI 的剧情指导） |
| prerequisites | TEXT | | 前置节点 ID（JSON 数组） |
| branch_options | TEXT | | 分支选项（JSON 数组） |

### 7.2 数据关系图

```
user_settings (1)
  ├── world_id → world_settings (N)
  ├── path_id → world_settings (N, category='path')
  └── current_chapter → chapters (1)

chapters (1) ──< story_segments (N)
diary_entries (1) ──> story_segments (1)
encounter_library (1) ──< encounter_log (N)
story_outline (N) — 按 chapter_number 关联 chapters
```

### 7.3 数据导出 JSON 格式

```json
{
  "version": "1.0",
  "export_date": "2026-07-06T12:00:00",
  "user_settings": { ... },
  "diary_entries": [ ... ],
  "chapters": [ ... ],
  "story_segments": [ ... ],
  "encounter_log": [ ... ]
}
```

导出时不包含 `world_settings`、`encounter_library`、`story_outline`（这些是预置配置，导入时从配置文件恢复）。

---

## 八、故事引擎

### 8.1 章节划分（修正后）

第一版 90 天分为 6 个章节段，序章和终章较短，中间 4 章各 20 天：

| 章节号 | 名称 | 天数范围 | 天数 | 定位 |
|--------|------|---------|------|------|
| 0 | 序章 | 第 1-5 天 | 5 | 主角觉醒，获得占卜家途径非凡能力 |
| 1 | 第一章 | 第 6-25 天 | 20 | 适应与探索，建立基础，加入塔罗会 |
| 2 | 第二章 | 第 26-45 天 | 20 | 冲突升级，遭遇第一个重大挑战 |
| 3 | 第三章 | 第 46-65 天 | 20 | 危机与转折，途径晋升 |
| 4 | 第四章 | 第 66-85 天 | 20 | 高潮与对抗，主线冲突爆发 |
| 5 | 终章 | 第 86-90 天 | 5 | 结局与收束，故事完结 |

### 8.2 天数追踪逻辑

`current_day` 的递增规则：

- 用户确认通过一段故事后，`current_day` +1
- 断更不自动递增（用户不记日记，`current_day` 不变）
- 用户重新打开 App 后，系统检查：上次记录是第 N 天，现在直接从第 N+1 天继续
- 不按自然日历追踪，按用户实际记录天数推进
- `current_day` 达到 90 时，故事完结，生成终章总结

### 8.3 章节边界检测

系统在每次段落定稿后检查：

```
if current_day == chapter.end_day:
    触发章节完成流程
    1. AI 生成本章摘要（200-300 字）存入 chapters.summary
    2. chapters.status 更新为 'completed'
    3. 创建下一章记录，status='ongoing'
    4. 下一段故事 Prompt 中加入"新章节开篇"指令
```

### 8.4 断更处理

当用户某天没有记录日记，但主动点击"生成"时（或次日继续记录时），系统检测到间隔：

- **断更 1-3 天**：AI 生成一段符合世界观的过渡剧情（如"主角在塔罗会度过了平静的几天"），自然衔接
- **断更超过 3 天**：在过渡剧情中加入"期间似乎错过了某些线索"的叙事，作为轻微方向偏离，但不影响主线
- **断更处理不消耗 `current_day`**：断更天数不推进剧情进度，仅生成过渡内容
- 不在 UI 层做断更惩罚、不发送推送催促

### 8.5 随机奇遇机制

除用户输入映射外，每周（7 天）随机触发 1-2 次世界观内奇遇：

**触发逻辑**：
- 每 7 天检查一次：生成随机数 0-1，<0.7 触发 1 次奇遇，<0.3 触发 2 次
- 从 `encounter_library` 中筛选：`min_chapter <= 当前章节 AND max_chapter >= 当前章节 AND min_path_level <= 当前等级`
- 从符合条件的奇遇中随机选取 1 个
- 检查 `encounter_log` 确保不重复触发同一奇遇

**奇遇与日记的关系**：
- 奇遇在当日日记映射的剧情基础上叠加，不替代日记内容
- 奇遇内容作为"额外事件"注入 Prompt，AI 将其自然融入当天剧情
- 奇遇不改变主线大方向，但可能引入新 NPC、新线索、新物品

### 8.6 故事结局逻辑

当 `current_day` 达到 90 时：

1. 终章最后一段故事生成时，Prompt 中加入"这是故事的最终章，请给出令人满意的结局"指令
2. AI 回收之前埋下的伏笔（依赖摘要记忆中的关键事件）
3. 生成终章总结（500-800 字），存入 `chapters.summary`
4. `story_started` 标记为完成状态
5. 首页显示"故事已完结"标签，用户仍可重新阅读所有章节
6. 提供"开始新故事"选项（重置数据后重新开始）

---

## 九、AI Prompt 架构

### 9.1 Prompt 分层与组装顺序

每次故事生成调用的完整 Prompt 由以下部分按顺序组装：

```
[System Prompt]          — 世界观基础 + 文风要求 + 输出格式（固定）
[章节阶段指令]            — 当前章节的剧情指导（半固定）
[摘要记忆]               — 已完成章节的摘要（动态）
[档案记忆]               — RAG 检索到的世界观设定（动态）
[即时上下文]             — 近 3 天剧情原文（动态）
[当日日记 + 映射结果]     — 用户输入和映射后的世界观行为（动态）
[奇遇注入]（如有）        — 当日触发的随机奇遇（动态）
[生成指令]               — 具体生成要求（固定）
```

### 9.2 各层 Prompt 模板骨架

#### 系统 Prompt

```
你是一位精通《诡秘之主》世界观的小说作家。你正在为用户创作一部以用户为主角的连载小说。

世界观基础设定：
- 维多利亚时代风格的架空世界，存在非凡者和非凡特性
- 22 条神之途径，每条途径有 9 个等级（序列 9 到序列 0）
- 主角当前途径：占卜家（序列 9）
- 占卜家能力：灵视、占卜、星象、塔罗牌

文风要求：
- 第三人称叙事，以主角名"{hero_name}"称呼主角
- 诡秘、悬疑、克苏鲁风格，带有维多利亚时代的氛围
- 每段 800-1200 字，有场景描写、心理活动和对话
- 保持连载小说的节奏感，有悬念和伏笔

输出格式：
- 直接输出小说正文，不要输出任何解释或元信息
- 不要输出标题，直接从正文开始
```

#### 章节阶段指令

```
当前故事进度：
- 第 {current_day} 天 / 共 90 天
- 当前章节：第 {chapter_number} 章「{chapter_title}」
- 章节定位：{chapter_purpose}

本章剧情指导：
{outline_content}

当前阶段需要推进的剧情方向：
{branch_guidance}
```

#### 摘要记忆

```
前情提要（之前章节摘要）：

序章摘要：{chapter_0_summary}
第一章摘要：{chapter_1_summary}
...
```

#### 档案记忆（RAG 检索结果）

```
相关世界观设定（供参考）：

【途径】{path_setting}
【力量体系】{power_system}
【地点】{location_setting}
【人物】{character_setting}
```

#### 即时上下文

```
最近 3 天的故事原文：

【第 {day-2} 天】
{content_day_minus_2}

【第 {day-1} 天】
{content_day_minus_1}

【第 {day} 天（昨日）】
{content_day_minus_1}
```

#### 当日日记 + 映射结果

```
今日主角的真实经历（已映射为世界观行为）：

用户日记原文：{raw_text}

行为映射结果：
- {behavior_1} → {world_behavior_1}（如：学习 2 小时 → 研读神秘学典籍两个时辰）
- {behavior_2} → {world_behavior_2}

请根据以上映射结果，将今日经历融入主角的故事中。
```

#### 奇遇注入（如有）

```
今日额外事件（随机奇遇）：
{encounter_content}

请将此事件自然融入今日剧情，不要生硬插入。
```

#### 生成指令

```
请生成今日（第 {current_day} 天）的故事段落。要求：
1. 将今日经历映射为世界观内的行为，融入连续剧情
2. 与最近 3 天的故事自然衔接
3. 符合当前章节的剧情走向
4. 字数 800-1200 字
5. 如有奇遇事件，自然融入
6. 保持悬念和连载感
```

#### 审查 Prompt（内部调用）

```
请审查以下小说段落，检查以下维度：

1. 世界观一致性：是否有不符合《诡秘之主》设定的内容？
2. 行为映射正确性：用户行为是否被正确映射到世界观行为？
3. 逻辑矛盾：是否有前后矛盾的情节？
4. 文风一致性：是否符合诡秘悬疑风格？
5. 衔接性：是否与前 3 天剧情自然衔接？
6. 节奏感：是否有适当的悬念和起伏？

待审查段落：
{generated_content}

请输出 JSON 格式：
{
  "passed": true/false,
  "issues": ["问题1", "问题2", ...],
  "suggestions": "修改建议"
}
```

#### 摘要生成 Prompt

```
请为以下章节生成一段 200-300 字的剧情摘要，包含：
- 核心事件
- 关键转折
- 人物关系变化
- 未解决的悬念

章节内容：
{chapter_full_content}

请直接输出摘要，不要输出其他内容。
```

### 9.3 AI 适配器接口规范

所有 AI 厂商适配器需实现统一接口：

```typescript
interface AIAdapter {
  // 厂商标识
  providerId: string;
  providerName: string;

  // 测试连接
  testConnection(apiKey: string, baseUrl?: string): Promise<{
    success: boolean;
    error?: string;
  }>;

  // 对话补全（核心方法）
  chat(params: {
    apiKey: string;
    baseUrl?: string;
    systemPrompt: string;
    userPrompt: string;
    temperature?: number;
    maxTokens?: number;
  }): Promise<{
    success: boolean;
    content?: string;
    error?: string;
    tokensUsed?: number;
  }>;
}
```

第一版需实现的适配器：

| 适配器 | 厂商 | API 格式 | 说明 |
|--------|------|---------|------|
| DeepSeekAdapter | DeepSeek | OpenAI 兼容 | 默认推荐，价格低，中文好 |
| OpenAIAdapter | OpenAI | 原生 | 国际通用 |
| KimiAdapter | Moonshot | 原生 | 国内可选 |
| QwenAdapter | 通义千问 | 原生 | 国内可选 |

---

## 十、技术实现要点

### 10.1 本地存储方案

- 使用 sql.js（SQLite 的 JavaScript 版本）在浏览器/App 中运行
- 数据库文件保存在 App 的持久化存储目录中（Capacitor 的 Filesystem API）
- App 启动时从文件加载数据库到内存，操作时定期写回文件
- 写回策略：每次 `finalized` 状态变更后立即写回，其他操作每 5 次写回一次
- 提供导出 JSON / 导入 JSON 功能用于数据迁移

### 10.2 数据库初始化与迁移

- 首次启动：创建所有表，写入默认配置
- 版本迁移：`app_config` 中记录 `db_version`，每次启动检查版本号，执行迁移脚本
- 预置数据：首次启动时从配置文件加载 `world_settings`、`encounter_library`、`story_outline`

### 10.3 API Key 安全存储

- 使用 Web Crypto API 的 AES-GCM 算法加密 API Key
- 加密密钥派生自设备唯一标识 + 固定盐值（PBKDF2）
- 加密后存入 `user_settings.api_key_encrypted`
- 调用 AI 时从数据库读取并解密，仅存在内存中，不写入日志
- 导出数据时不导出 API Key

### 10.4 并发控制

- 生成过程中禁用"生成故事"按钮，防止重复提交
- 生成过程中用户离开页面，后台继续生成，返回后恢复状态
- 使用单例锁：同一时间只允许一个生成任务运行
- 生成任务状态持久化到 `story_segments.status`，App 崩溃后可恢复

### 10.5 语音输入实现

- 优先使用 Web Speech API（`SpeechRecognition`）
- 打包成 App 后如不支持，通过 Capacitor 的 `@capacitor-community/speech-recognition` 插件调用原生能力
- 语音识别语言：zh-CN（中文）
- 识别结果实时填入文本框，用户可修改

### 10.6 离线可用性

- 已生成的章节、用户设置支持离线查看
- AI 生成需要联网（调用 API）
- 断网时点击"生成故事"提示"需要网络连接才能生成故事"
- 断网时仍可阅读已有章节、修改设置、导出数据

### 10.7 内容长度控制

- 每日故事段落：800-1200 字（通过 Prompt 中的字数指令控制）
- 章节摘要：200-300 字
- 日记输入：最大 5000 字
- 90 天总字数预估：800×90 = 72000 字（约 7 万字长篇小说）

---

## 十一、错误处理

### 11.1 错误码体系

| 错误码 | 类型 | 说明 | 用户提示 |
|--------|------|------|---------|
| E001 | API_KEY_INVALID | API Key 无效 | "API Key 无效，请检查设置" |
| E002 | API_KEY_EXPIRED | API Key 过期 | "API Key 已过期，请更新" |
| E003 | API_RATE_LIMIT | 触发频率限制 | "请求过于频繁，请稍后重试" |
| E004 | API_QUOTA_EXCEEDED | 额度用尽 | "AI 额度已用完，请充值或更换 Key" |
| E005 | API_TIMEOUT | 请求超时（>60s） | "生成超时，请检查网络后重试" |
| E006 | API_NETWORK_ERROR | 网络错误 | "网络连接失败，请检查网络" |
| E007 | API_RESPONSE_ERROR | 响应格式错误 | "AI 返回异常，请重试" |
| E008 | DB_INIT_ERROR | 数据库初始化失败 | "数据初始化失败，请重启 App" |
| E009 | DB_WRITE_ERROR | 数据库写入失败 | "数据保存失败，请重试" |
| E010 | DB_CORRUPT_ERROR | 数据库损坏 | "数据损坏，请从备份恢复或重置" |
| E011 | SPEECH_NOT_SUPPORTED | 语音识别不支持 | "当前设备不支持语音输入，请使用文字" |
| E012 | SPEECH_RECOGNITION_FAILED | 语音识别失败 | "语音识别失败，请重试或手动输入" |
| E013 | EXPORT_FAILED | 导出失败 | "导出失败，请重试" |
| E014 | IMPORT_INVALID_FORMAT | 导入格式错误 | "文件格式不正确，请选择有效的备份文件" |

### 11.2 AI 生成失败处理策略

```
用户点击"生成故事"
  │
  ▼
调用 AI API
  │
  ├─ 成功 → 进入审查循环 → 输出预览
  │
  └─ 失败
       ├─ E001/E002（Key 问题）→ 提示检查 Key，跳转设置页
       ├─ E003（频率限制）→ 等待 5 秒自动重试，最多 2 次
       ├─ E004（额度用尽）→ 提示充值或更换 Key
       ├─ E005（超时）→ 提示重试，不消耗重生成次数
       ├─ E006（网络）→ 提示检查网络，可重试
       └─ E007（响应错误）→ 自动重试 1 次，仍失败则提示
```

### 11.3 内部审查失败处理

- 审查轮次达到最大重试次数（3 次）仍未通过：直接输出当前最优版本（最后一次修改稿）
- 审查过程中 AI 调用失败：跳过该轮审查，直接进入下一轮或输出
- 保证用户始终能拿到一个结果，不会卡在"生成中"状态

---

## 十二、性能要求

| 指标 | 目标值 | 说明 |
|------|--------|------|
| AI 生成响应时间 | < 30 秒 | 从点击"生成"到预览页展示结果 |
| App 冷启动时间 | < 3 秒 | 从点击图标到首页可交互 |
| 页面切换响应 | < 300ms | 页面间跳转 |
| 数据库写回 | < 500ms | 不阻塞 UI |
| 章节加载 | < 200ms | 打开历史章节 |
| 单次 AI 调用 Token | < 12000 | 输入+输出总计 |
| 本地存储上限 | < 50MB | 90 天全部数据 |

---

## 十三、测试策略

### 13.1 功能测试

| 测试项 | 测试内容 | 通过标准 |
|--------|---------|---------|
| 引导流程 | 首次使用完整走通 | 5 步引导全部完成，跳转首页 |
| 日记输入 | 文字/语音/标签 | 三种输入方式均正常工作 |
| AI 生成 | 完整生成流程 | 生成 → 审查 → 预览 → 定稿全流程通过 |
| 重生成 | 用户重试 3 次 | 3 次后按钮禁用，提示手动编辑 |
| 手动编辑 | 修改后保存 | 修改内容正确存入，标记 is_edited |
| 章节边界 | 第 5/25/45/65/85 天 | 正确触发摘要生成和新章节创建 |
| 断更处理 | 跳过 1-3 天后继续 | 生成过渡剧情，进度不跳 |
| 故事完结 | 第 90 天 | 正确生成结局，标记完结 |
| 数据导出/导入 | 导出后导入到新设备 | 数据完整恢复 |
| 数据重置 | 重置后重新开始 | 所有数据清空，回到引导页 |

### 13.2 AI 质量测试

| 测试项 | 测试内容 | 通过标准 |
|--------|---------|---------|
| 世界观一致性 | 生成内容是否符合诡秘设定 | 无明显设定错误 |
| 行为映射准确性 | 日常行为是否正确映射 | 映射合理，不生硬 |
| 剧情连贯性 | 跨天剧情是否衔接 | 近 3 天剧情有自然衔接 |
| 文风一致性 | 诡秘悬疑风格是否保持 | 文风统一，不出戏 |
| 摘要质量 | 章节摘要是否准确 | 涵盖核心事件，200-300 字 |
| 审查有效性 | 审查是否发现真实问题 | 修改后质量有提升 |

### 13.3 边界测试

- 第 1 天生成（无上下文）
- 章节边界日生成
- 断更 1/3/7 天后恢复
- 连续重生成 3 次
- API Key 错误时的错误提示
- 网络断开时的行为
- 90 天完结流程
- 超长日记输入（5000 字）
- 空日记输入（仅标签无文字）

---

## 十四、项目结构

```
mystory/
├── index.html                      # App 入口
├── capacitor.config.json           # Capacitor 配置
├── package.json
├── vite.config.js                  # Vite 构建配置
│
├── src/
│   ├── main.js                     # Vue 应用入口
│   ├── App.vue                     # 根组件
│   │
│   ├── router/                     # 路由
│   │   └── index.js
│   │
│   ├── views/                      # 页面组件
│   │   ├── Onboarding.vue          # 引导页
│   │   ├── Home.vue                # 首页/阅读器
│   │   ├── DiaryInput.vue          # 日记输入
│   │   ├── Preview.vue             # AI 生成预览
│   │   ├── ChapterReader.vue       # 章节阅读
│   │   ├── Settings.vue            # 设置首页
│   │   ├── SettingsWorld.vue       # 世界观设置
│   │   ├── SettingsApi.vue         # API Key 设置
│   │   └── SettingsData.vue        # 数据管理
│   │
│   ├── components/                 # 通用组件
│   │   ├── LoadingSpinner.vue      # 加载动画
│   │   ├── ChapterList.vue         # 章节目录
│   │   ├── BehaviorTags.vue        # 行为标签选择器
│   │   ├── VoiceInput.vue          # 语音输入组件
│   │   └── ErrorToast.vue          # 错误提示
│   │
│   ├── core/                       # 核心业务逻辑
│   │   ├── StoryEngine.js          # 故事引擎（总调度）
│   │   ├── DiaryParser.js          # 日记解析器
│   │   ├── MappingEngine.js        # 映射规则引擎
│   │   ├── ChapterManager.js       # 章节管理器
│   │   ├── EncounterEngine.js      # 奇遇引擎
│   │   └── ProgressTracker.js      # 进度追踪器
│   │
│   ├── ai/                         # AI 编排层
│   │   ├── PromptBuilder.js        # Prompt 拼装器
│   │   ├── ReviewLoop.js           # 审查循环器
│   │   ├── SummaryGenerator.js     # 摘要生成器
│   │   └── adapters/               # AI 适配器
│   │       ├── AIAdapter.js        # 接口定义
│   │       ├── DeepSeekAdapter.js
│   │       ├── OpenAIAdapter.js
│   │       ├── KimiAdapter.js
│   │       └── QwenAdapter.js
│   │
│   ├── memory/                     # 记忆管理层
│   │   ├── ImmediateContext.js     # 即时上下文提供器
│   │   ├── SummaryMemory.js        # 摘要记忆提供器
│   │   └── RAGRetriever.js         # RAG 检索器
│   │
│   ├── db/                         # 数据层
│   │   ├── Database.js             # SQLite 初始化与管理
│   │   ├── migrations/             # 数据库迁移脚本
│   │   ├── repositories/           # 数据访问层
│   │   │   ├── UserRepository.js
│   │   │   ├── DiaryRepository.js
│   │   │   ├── ChapterRepository.js
│   │   │   ├── SegmentRepository.js
│   │   │   └── WorldRepository.js
│   │   └── DataExporter.js         # 数据导出/导入
│   │
│   ├── config/                     # 配置文件
│   │   ├── worlds/                 # 世界观配置包
│   │   │   └── lord_of_mysteries/  # 诡秘之主
│   │   │       ├── world.json      # 世界观基础设定
│   │   │       ├── paths.json      # 途径配置
│   │   │       ├── mappings.json   # 行为映射规则
│   │   │       ├── outline.json    # 剧情大纲
│   │   │       ├── encounters.json # 奇遇库
│   │   │       └── settings.json   # 世界观设定档案
│   │   └── app.json                # 应用全局配置
│   │
│   ├── utils/                      # 工具函数
│   │   ├── Crypto.js               # AES-GCM 加密
│   │   ├── TokenCounter.js         # Token 计数器
│   │   └── ErrorHandler.js         # 错误处理
│   │
│   └── assets/                     # 静态资源
│       ├── styles/
│       └── images/
│
└── docs/                           # 文档
    ├── MyStory-功能规划文档.md      # 本文档
    ├── MyStory-剧情大纲.md          # 配套：三个月剧情大纲
    ├── MyStory-映射规则表.md        # 配套：行为大类映射规则
    ├── MyStory-AI-Prompt模板.md     # 配套：完整 Prompt 模板
    └── MyStory-世界观资料.md        # 配套：诡秘之主设定资料
```

### 14.1 世界观配置包格式

每个世界观是一个独立目录，包含以下 JSON 文件：

**world.json**（世界观基础设定）
```json
{
  "world_id": "lord_of_mysteries",
  "world_name": "诡秘之主",
  "description": "维多利亚时代风格的架空世界...",
  "style_guide": "诡秘、悬疑、克苏鲁风格...",
  "default_path": "seer",
  "default_duration": 90
}
```

**mappings.json**（行为映射规则）
```json
{
  "world_id": "lord_of_mysteries",
  "mappings": [
    {
      "behavior": "学习",
      "world_behavior": "研读神秘学典籍",
      "keywords": ["学习", "看书", "上课", "读书", "复习"],
      "description": "将学习行为映射为研读神秘学相关书籍"
    }
  ]
}
```

**paths.json**（途径配置）
```json
{
  "world_id": "lord_of_mysteries",
  "paths": [
    {
      "path_id": "seer",
      "path_name": "占卜家",
      "description": "...",
      "levels": [
        { "level": 9, "name": "序列 9", "abilities": ["灵视", "占卜"] }
      ]
    }
  ]
}
```

---

## 十五、开发计划与里程碑

### 15.1 里程碑

| 阶段 | 内容 | 交付物 | 预估时间 |
|------|------|--------|---------|
| M1 | 配套文档编写 | 剧情大纲、映射规则表、Prompt 模板、世界观资料 | 3 天 |
| M2 | 项目框架搭建 | Vite + Vue 3 项目、SQLite 初始化、路由、配置加载 | 2 天 |
| M3 | 数据层开发 | 全部 Repository、数据库迁移、导出导入 | 2 天 |
| M4 | AI 编排层开发 | Prompt 拼装、AI 适配器、审查循环、摘要生成 | 3 天 |
| M5 | 记忆管理层开发 | 即时上下文、摘要记忆、RAG 检索 | 2 天 |
| M6 | 核心业务逻辑 | 故事引擎、日记解析、映射引擎、章节管理、奇遇引擎 | 3 天 |
| M7 | 前端页面开发 | 引导页、首页、日记输入、生成预览、设置页、数据管理 | 4 天 |
| M8 | 联调与测试 | 全流程联调、边界测试、AI 质量测试 | 3 天 |
| M9 | App 打包 | Capacitor 打包、真机测试 | 2 天 |
| M10 | 调优与交付 | Prompt 调优、文风调整、Bug 修复 | 3 天 |

总预估：约 27 个工作日（5-6 周）

### 15.2 关键路径

M1（文档）→ M2（框架）→ M3（数据层）→ M4（AI 层）→ M6（核心逻辑）→ M7（前端）→ M8（联调）→ M9（打包）

M5（记忆层）可与 M6 并行，M10（调优）在 M8 后进行。

---

## 十六、风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| AI 生成质量不稳定 | 中 | 高 | 多轮审查 + Human-in-the-Loop 允许用户编辑和重生成 |
| 不同模型输出差异大 | 高 | 中 | Prompt 模板做模型适配，测试覆盖所有支持的模型 |
| Token 超限 | 低 | 高 | Token 预算管理，即时上下文从 3 天降为 2 天 |
| sql.js 性能问题 | 低 | 中 | 数据量小（<50MB），性能足够；写回策略优化 |
| 语音识别不支持 | 中 | 低 | 降级为纯文字输入，不影响核心功能 |
| 用户 API Key 泄露 | 低 | 高 | AES-GCM 加密存储，不导出，不记日志 |
| 数据丢失（App 卸载） | 中 | 高 | 定期提示用户导出备份 |
| 诡秘之主版权问题 | 中 | 中 | 第一版仅用于个人使用，不公开发布；AI 生成内容为原创剧情 |
| 长期使用存储增长 | 低 | 低 | 90 天总数据 <50MB，无需特别处理 |

---

## 十七、后续配套文档清单

| # | 文档 | 说明 | 状态 |
|---|------|------|------|
| 1 | `MyStory-剧情大纲.md` | 三个月主线框架 + 分支节点 + 高潮设计 + 奇遇库 | 待编写 |
| 2 | `MyStory-映射规则表.md` | 日常行为类别 → 诡秘世界观行为的完整映射规则 | 待编写 |
| 3 | `MyStory-AI-Prompt模板.md` | 各层 Prompt 的完整文本（含变量占位符） | 待编写 |
| 4 | `MyStory-世界观资料.md` | 诡秘之主核心设定结构化数据（途径、力量体系、地点、人物） | 待编写 |

---

## 附录：3 轮审查记录

### 第 1 轮：完整性审查

发现并补全以下 24 项缺失：

1. 首次使用引导流程（Onboarding）— 新增 6.2 节
2. 故事段落状态机 — 新增 5.2 节
3. API 适配器接口规范 — 新增 9.3 节
4. 错误处理策略 — 新增第十一章
5. 安全与隐私（API Key 加密） — 新增 10.3 节
6. 性能要求 — 新增第十二章
7. 测试策略 — 新增第十三章
8. 项目目录结构 — 新增第十四章
9. 世界观配置包格式 — 新增 14.1 节
10. Token 预算管理 — 新增 5.5 节
11. 并发控制 — 新增 10.4 节
12. 内容长度控制 — 新增 10.7 节
13. 故事结局逻辑 — 新增 8.6 节
14. 天数追踪逻辑 — 新增 8.2 节
15. 章节边界检测 — 新增 8.3 节
16. 页面导航结构 — 新增 4.2 节
17. 路由设计 — 新增 4.3 节
18. 奇遇库数据结构 — 新增 encounter_library 表
19. 奇遇触发记录 — 新增 encounter_log 表
20. 剧情大纲节点结构 — 新增 story_outline 表
21. 数据导出 JSON 格式 — 新增 7.3 节
22. 数据库迁移机制 — 新增 10.2 节
23. 开发里程碑 — 新增第十五章
24. 风险与应对 — 新增第十六章

### 第 2 轮：一致性审查

修正以下 5 项矛盾：

1. **章节划分口径不统一**：原文"每 20 天为一章，共约 4-5 章"与章节表（6 段）矛盾。修正为"6 个章节段（序章 5 天 + 4 章各 20 天 + 终章 5 天）"，全文统一
2. **记忆层与数据表未对应**：三层记忆架构现在明确对应到 `story_segments.content`（即时）、`chapters.summary`（摘要）、`world_settings`（档案）
3. **段落状态不完整**：`story_segments.status` 从原来的 `draft/finalized` 扩展为 8 个状态的完整状态机
4. **内部审查与用户重试混淆**：明确区分 — 内部审查最多 3 轮（AI 自行），用户重生成最多 3 次（用户主动），两者独立计数
5. **天数追踪与日历关系未定义**：明确 `current_day` 按用户实际记录天数推进，不按自然日历，断更不自动递增

### 第 3 轮：可执行性审查

细化以下 8 项落地细节：

1. **SQL 表结构**：所有表增加了完整字段类型、约束、索引定义
2. **API 接口定义**：AIAdapter 接口用 TypeScript 明确定义方法和参数
3. **页面路由**：明确 9 个路由路径和对应页面
4. **Prompt 模板骨架**：9.2 节给出 8 种 Prompt 的完整骨架文本
5. **项目目录结构**：完整文件树，精确到每个文件
6. **配置文件格式**：world.json、mappings.json、paths.json 的 JSON 结构示例
7. **错误码体系**：14 个错误码 + 触发条件 + 用户提示文案
8. **里程碑时间表**：10 个里程碑，精确到每个阶段的交付物和预估天数
